# ゲーム棚 やること一覧

要件はここに番号で残す。会話に埋めない。**一度に一つだけ着手し、確かめてもらってから次へ。**
（進め方の取り決め: 覚え書き `numbered-backlog`）

## いま確かめてもらいたい（実装済み・未確認）

| # | こと | 中身 |
|---|---|---|
| A1 | 整形済みブランクディスク | FORMAT 不要。本物と FDI頭・BPB・FAT が一致することを確認済み |
| A2 | 揺らぎの記録 | ログに「中央/95%/最大/大きく空いた回数/荒れ%」。平均では見えない揺れを出す |
| A3 | 前回の組み合わせを覚える | 選択画面だけでなく**遊んでいる最中の入れ替えも**残す |
| A4 | 同じディスクを両ドライブに入れない | 覚え書きが壊れていても弾く（argv に同じ1枚が2回入っていた） |
| A5 | 動きの記録（設定→動きの記録） | 最新10回。端末・ブラウザ・CPU数つき。機械のせいかを見分ける |
| A6 | 音の周波数をブラウザに合わせる | `SampleHz` をその場で聞いて合わせる。音程のずれ対策 |
| A7 | マウスを速く | `MS_RAPID`。画面を引き伸ばした分の鈍さ対策 |
| A8 | 全画面と画面の拡大 | `max-width` だけで拡大していなかったのを直した |
| A9 | メニュー/マウスを直に呼ぶ | 作った鍵は本家の完全一致判定で弾かれるため |
| A10 | 未アップの本を押すと上げに行ける | 行き止まりにしない |
| A13 | **中継所が無くても遊べる** | Cloudflare が落ちた日の逃げ道。`fetchFile` が「中継所の直リンク → 公開リンク → 中継所なし（`file_open`/`file_read`）」の順に自動で降りる。**確かめ方: 設定 → 中継所 → 「やめる」→ 手元に無い本を遊ぶ。**（遅いが起動すれば成功）。机上の試験は `shelf-core/tools/test-routes.mjs` で6件通過済み |
| A12 | **いつでもディスクを選び直せる** | 1枚ものだと出していなかった。覚えた組み合わせが悪いと**抜け出せなくなっていた**（三国志2で詰んだ）。設定に「組み合わせを忘れる」も追加 |
| A13 | セーブディスクだけの本を遊ぶものに数えない | 「三国志2セーブディスク」が題名だけ「三国志2」になって遊べる本に見えていた。遊ぶもの 88→85 本 |
| A14 | **ディスクの道具箱**（設定 → ディスクの道具箱） | 差さずに中を見る／整形済みブランクを作る／複製／名前を変える。作ったものは play98 の「ほかから足す」に出ます |
| A15 | **10秒ごとの速さの並び**（設定 → 動きの記録） | 通しの平均だと「速くなったり重くなったり」が1つの数字に潰れて見えない。棒で並べた |
| A18 | **棚に取り寄せる／書庫へ下ろす** | 遊び始めると `/ゲーム棚/棚/<題名>/` ができ、本体・箱絵・メモ・セーブが入ります。下ろすと `/ゲーム棚/書庫/<題名>/` へ丸ごと移ります。**倉庫の元の置き場（`/EMU/ROM` など）はそのまま。** 最初は1本で試してください |
| A17 | **重複を片付ける**（倉庫の場所 → 重複を片付ける） | 場所を選んで「調べる」→ 同じ名前・同じ大きさの組が出る → 残す1本を確かめて「消す」。**大きさ違いは触りません。** 消す前に一覧と合計が出ます。**最初は狭い場所で試してください** |
| A16 | **pCloud から棚へ**（設定 → pCloud から棚へ） | 「pCloud の中を探す」→ 棚の外にある台帳の本が並ぶ → 選んで「棚へ移す」。**上げ直しません。** iPhone からもできます。<br>**まだ実際に動かしていません**（合鍵が要るため）。最初は1〜2本で試してください |
| A11 | 直リンクで渡してもらえない本の救済 | 「pCloud が中身を渡しません（直リンク）」で止まる本がある（維新の嵐）。中継所は公開リンク経由の道も持っているので、駄目なら降りる |

## 端末ごとに一度だけ要る設定

新しい端末（iPhone など）で開いたら:
1. pCloud につなぐ　2. 設定 → フォルダを選び直す → `/ゲーム棚`
3.（任意）設定 → 中継所に `https://gamedana.d-shino.workers.dev`

**3 は無くても遊べる**（2026-08-30 から）。取り寄せが遅くなるだけ。#17 を参照。

## これから（番号順に一つずつ）

| # | こと | 状態 |
|---|---|---|
| 1 | ~~セーブ／巻き戻し~~ | **済（本人確認済み）。** コアに `np2web_savestate` / `np2web_loadstate` を足し、帯に「セーブ」「戻す」（3枠）。端末に残るので次に開いても戻せる |
| 2 | **ディスクユーティリティ** | **画面まで出来た（2026-08-30・確認待ち＝A14）。** 設定 →「ディスクの道具箱」。整形済みブランクを作る／複製／名前を変える／消す／**差さずに中を見る**（名札・空き・入っているファイル一覧・日付）／中のファイルを取り出す。実物6枚で確かめた（`ATOK7A.SYS` `IO.SYS` `AYA3.EXE` を正しく読み、`BLANKDISK.fdi` の空き 1221KB がこちらで作るブランクと一致）。<br>直した誤り: `open()` が**拡張子だけで頭の大きさを決めていた**。道具箱で作る名前には拡張子が無いので頭を外し損ね、自分で作ったディスクを自分で読めなかった。**当ててみて読めたところを採る**ようにした。<br>まだ: 中身の**書き換え**（入れる・消す・名前を変える）は読み取りのみ |
| 3 | ~~パッケージ画像とジャンル~~ | **済（2026-08-30）。** `tools/fetch-covers.py`。3段に降りる: ①手元の openvgdb を**中身の md5**で引く（**素のファイルの md5 では1件も当たらない**。iNES の16バイト／写し取り用の512バイトを外す）②題名で照合 ③**libretro のサムネイル置き場**（GitHub・鍵不要・No-Intro の名前が鍵）。<br>結果: ファミコン系 **箱絵 194/253・ジャンル 177**、PC-98 **箱絵 8/85**。<br>棚に**ジャンルの絞り込み**を追加（音楽棚と同じ形。多い順に並べ、件数つき）。<br>**日本語版 Wikipedia は箱絵を載せていない**（非自由画像を原則置かない）ので使えない。試して確かめた。<br>PC-98 は libretro に光栄ものがほとんど無い（三國志Vと信長の天翔記だけ）。残りは手で入れる道が要る |
| 4 | **速さと音の揺らぎの根治** | 本家は SDL の ScriptProcessor をやめ **AudioWorklet**（遅延30ms台）。コアの外側なので安全に試せる |
| 5 | ~~GitHub Pages へ公開~~ | **済（2026-08-30）** https://dshino9.github.io/pcloud-games/ 道具棚にも登録 |
| 9b | **ドクターマリオ・F-ZERO が1分ほどで落ちる**（2026-08-30） | 速さの手当ては効いた（本人「動きは良かった」）が、**速くなったうえで落ちる**ようになった。<br>渡していた `EJS_defaultOptions` は本家の中身を読んで確認 —— `shader`/`rewindEnabled`/`vsync` はどれも正しい名前で、`vsync: enabled` は**もともとの既定と同じ**。設定は無実。<br>**手元では再現できない。** ヘッドレスも隠れた頁扱いになり、90秒回してもコマ数0・ヒープ4MBで張り付く（描いていないので当然）。<br>→ **記録を棚へ自動で上げるようにした。** 遊んでいる最中20秒ごとに `/ゲーム棚/_記録/<端末>.json` へ。落ちても直前までは残る。ちゃんと閉じたときだけ「終わった」印が付くので、**印の無い記録＝落ちた**と読める。しくじりの言い分（`onerror`/`unhandledrejection`）も残す。<br>読むのは `python3 tools/runs.py`（合鍵は本人が一度だけ入れる） |
| 9 | **iPhone でファミコン・スーファミが激重** | 出だしは良く、途中から落ちる。まず**測れるように**した（`play.html` にもコマ数・揺らぎ・端末を記録）。手当て第一弾: 遊ぶあいだ棚を畳んで箱絵の重さを手放す。**数字を見てから次の手** |
| 10 | ~~横持ちでボタンがはみ出る~~ | **済。** 縦の safe-area しか見ていなかった。左右にも余白を取る。ぼかしは横持ちで高くつくので狭い画面では使わない |
| 13 | ~~コアの js と wasm が食い違う~~ | **済。** 出来上がりを後から改名していたので、JSPI 版の JS が ASYNCIFY 版の wasm を読んでいた（`missing Wasm export` で起動が死ぬ）。**器ごと分けた**（`np2/jspi/` `np2/asyncify/`）。wasm にも版を付ける |
| 14 | ~~名前の重複で覆いが消えない~~ | **済。** 読み込み中の覆いと帯のボタンが同じ `load` を名乗っていた。覆いの見た目がボタンに効いて帯を覆い尽くし、名前で引くとボタンが先に見つかるので**覆いを隠す命令がボタンに当たっていた**。iPhone で操作できなかった正体はこれ |
| 15 | ~~狭い画面で帯のボタンが隠れる~~ | **済。** 横に流していたので見えなかった。狭い画面では折り返す。帯の高さに合わせて絵と鍵盤の位置も動かす |
| 16 | **「コア」と呼ぶ**（本人の指定） | 「心臓」は変。置き場と覚え書きを全部言い換えた（2026-08-30）**済** |
| 11 | ~~タッチ端末で PC-98 が操作できない~~ | **済（本人確認済み）。** iPhone にはマウスも鍵盤も無い。**指をマウスに訳す**（滑らす＝動かす、触って離す＝左、長押し＝右）と、**画面のキーボード**（帯の「キー」）を足した。本体メニューもこれで押せる。**確認待ち** |
| 12 | **スマホで全画面が効かない** | iOS の Safari は**動画以外を全画面にできない**（Fullscreen API が無い）。代わりに上の帯を畳んで絵に画面を全部渡す（左上の丸で戻す）。**本当の全画面はホーム画面に追加して開く**。高さは `100dvh` に |
| 6 | 残り PC-98 47本を上げる | `pc98-pick.txt` に足して `--pick` |
| 7 | N64 6本・DS 12本 | 重いので保留のまま |
| 8 | 箱絵：ファミコン系 **251/253**（残りは BIOS）、**PC-98 80/85**（2026-08-30） | 画像検索を最後の段に足した（`--search`）。**目で見て 57枚すべて正しい箱だと確かめた。**<br>**踏んだ罠が2つ。** ①**絵だけ見て選ぶと何でも通る。** 大きさと縦横比だけで採ったら、提督の決断2にルンバ、群雄三国志にF1、項劉記に日本地図が入った。63枚入れて全部捨てた。→ **検索結果に付いている題名と本の名前を突き合わせる**（ローマ数字は算用数字に直して部分一致）。②**Bing の HTML を続けて掻き取ると相手が壊れる。** 76回で水滸伝に Minecraft、三國志2に類語辞典を返し始めた。→ **DuckDuckGo の JSON（題名・幅・高さが付く）に替えた。**<br>言い方を3通り試す（「PC-9801 パッケージ 箱」→「PC98 パッケージ」→「PC-9801」）。<br>名前が当たらない分は3つの手で埋めた: ①**末尾の括弧を落とす**（`Lemmings (J)` `Rance 3 (1991)(Alice Soft)`）②**半角カナを全角に**（`ﾄﾞﾗｺﾞﾝﾎﾞｰﾙ`）③**読みの表 `pc98-alias.txt`**（`信長全国版`→信長の野望 全国版、`水滸伝A改良版`→水滸伝 天命の誓い など18件）。<br>**並びの通りに採らない。** 検索はその本の絵を上位に出すが、PC-98 版の箱とは限らない（Popful Mail でメガCD版、Ys2 で MSX2 版を掴んだ）。題名に `9801`/`PC98` が入っているものを先に見る。<br>残り3本（`Aya3` `Aya3M` `PC-9801 Game Pack Vol.1`）は正体が分からない。**手で入れる口**が要る |
| 17 | ~~中継所が単一障害点~~ | **済（2026-08-30・確認待ち＝A13）。** Cloudflare が落ちる／規約で止められる／自分が消す、のどれでも棚が死んでいた。<br>直したこと: ①`shelf-core/wrangler.toml` を追加（**立て方が Git に無く、消したら戻せなかった**）②`fetchFile` に3本目の道（中継所なし）を追加し、`play.html`・`play98.html` を両方そこへ寄せた ③机上試験 `tools/test-routes.mjs`（外に繋がず6件）<br>直した誤り: 覚え書き3か所にあった「`file_open` は `2003` で使えない」は誤り。`flags:0`＋`fileid` で通る |
| 18 | **予備の中継所（Deno Deploy）** | `relay.js` は `export default { fetch }` 一枚なので、Deno Deploy はほぼ無改造で通るはず。設定に「予備の中継所」欄を足し、`relayAlive()` が落ちたら切り替える。#17 で「遅いが動く」道は確保できたので、これは**速いまま生き延びる**ための保険 |
| 54 | ~~棚に入れた版を「正」にする~~ | **済（2026-08-31・本人の気づき「取り寄せをすることで表示上の重複問題も一部解決するよな」）。** そのとおりで、**取り寄せた時点でどれを使うかは決まっている**ので、倉庫側の版違いをもう並べる必要がない。<br>→ 束の代表は **①棚にある版 ②選んだ版 ③素の名前に近いもの** の順。棚にある版があれば `4版` の札を消し、代わりに **`棚: [!]`** のようにどの版かを添える。押しても版を聞かない（決まっているので）。<br>試した: 取り寄せ前 `いっき（4版）` → 取り寄せ後 `いっき / 棚: [!]`・代表がその版に入れ替わる |
| 52 | ~~版違いをまとめる／メーカー別フォルダで選ぶ／PC-98 をもっと探す~~ | **済（2026-08-31）。**<br>**①版違いをまとめた（本人「ファミコンは版違いが複数あるので表示はまとめて」）。** 倉庫には `4人打ち麻雀` `[a1][b1]` `(FMG Pirate) [p1]` `[o1]` のように同じ本の版がいくつも入っていて、3380本の多くはこれ。**棚では1冊に見せ、版は遊ぶときに選ばせる**（選んだ版は覚える）。仕込みで 7本→3冊（`4人打ち麻雀（4版）` `1942（2版）` `グラディウス`）。<br>**②メーカー別フォルダで選べるようにした。** 走査で拾った**在処を本にも持たせ**、「倉庫のフォルダで畳む」を足した（`/ROM/パソコン/PC-98/光栄` の形のまま辿れる）。<br>**③PC-98 をもっと探す。** `/EMU/ROM` だけ見ていたので `/EMU/その他/…` の分を取りこぼしていた。→ **広いほうから試す**（`/EMU` が見つかればその下は足さない）。すでに下位が登録されていれば**外す**（同じ所を二度歩かないため） |
| 51 | ~~棚を倉庫の中の実体にする（本ごとのフォルダ）~~ | **済（2026-08-31・確かめ待ち＝A18）。** 本人の指定「取り寄せたやつは絵の取得、ディスクの組み合わせやセーブデータをフォルダ単位で別途格納」「棚から下げた時は周辺データを紐づけて一箇所にアーカイブ」。<br>**取り寄せると本ごとにフォルダができる**: `/ゲーム棚/棚/<題名>/` に ①**本体**（倉庫から**写す**。移さない —— 倉庫の整理を崩さないため）②`箱絵.jpg` ③`メモ.json`（ディスクの組み合わせ・機種・ジャンル）④`セーブ/`（書き換わったディスク。PC-98 のセーブはディスクそのものに書かれるので）。遊び始めたときに裏で走る。<br>**下ろすと `/ゲーム棚/書庫/<題名>/` へフォルダごと動く**（`renamefolder`）。1つずつ動かすと途中で落ちたとき散らばる。端末の控えも一緒に消す。**倉庫の元の置き場には触らない。**<br>作りは仕込みで確かめた: `棚/いっき/` を作り、本体を写し、箱絵とメモを置く → 下ろすと `書庫` へ1回の移動 |
| 50 | ~~箱絵の捜索を裏で／「取り寄せ」は棚へ~~ | **済（2026-08-31・本人「取り寄せってローカルではなく棚に取り寄せ」「全てのパッケージ絵の捜索も裏で」）。**<br>**①言葉。** 取り寄せ＝**倉庫から棚へ**。ローカルの話ではない。`取り寄せ済み`→**`棚にある`**、`取り寄せた分を消す`→**`棚から下ろす`**、空のときは `まだ棚に何も取り寄せていません`。<br>**②裏で箱絵を探す。** 倉庫が見えて 7171 本になり、台帳に絵は用意できない。→ **画面に出ている本の絵を裏で探して埋める**（libretro の置き場。CORS が開いているので**ブラウザから直に叩ける**ことを確認: DS・FC・SFC・MD で 4/4 当たった）。機種を15に広げた。名前は No-Intro の書き方なので、`0228 - ` や `[b1]` `(MODE7)` を落とし、`(Japan)` `(USA)` などを付けて数通り当てる。**見えている分だけ**（7171本の総当たりはしない）。探した結果は端末に覚えて二度探さない。<br>**③ブラウザからは画像検索ができない**（CORS）。続きは Mac 側の `tools/fetch-covers.py` の担当なので、**題名の一覧を倉庫の `_記録/棚.json` に書き出す**ようにした。`python3 tools/runs.py --shelf` で落とせる |
| 49 | ~~遊べない（2003 Access denied）／札の意味が逆~~ | **済（2026-08-31）。**<br>**①中継所が未設定だと詰む。** 中継所なしの道（`file_open`）はこの倉庫では **`2003 Access denied`** で塞がっている。未設定のまま遊ぶと、そこで止まる。→ **ゲーム棚用の中継所を既定で指す**（`https://gamedana.d-shino.workers.dev`）。一度「やめる」で空にしていると既定が効かないので、**空なら既定に落とす**。棚・`play.html`・`play98.html` の3か所とも。<br>**②札の意味を取り違えていた（本人の指摘）。** 耳読の `読める本` は**すでに手元にあるもの**、`蔵書` は**持っているもの全部**。こちらは `遊べる=倉庫にある` にしていた。<br>正しくは **`遊べる` ＝ 棚にある ＝ 取り寄せ済み（すぐ遊べる）**／**`倉庫` ＝ pCloud にある（取り寄せが要る）**。既定は `倉庫`（取り寄せ前は「遊べる」が 0 本なので）。`倉庫に無い` は 0 のとき出さない<br>**直しの直し（本人「一個もないの?」）。** 0 本は正しい数字だったが、2つ落ち度があった。①**意味を変えたのに鍵を変えていなかった** —— 前の選択（`遊べる`）が残っていて、既定の `倉庫` に切り替わらなかった。→ 鍵を `view2` にした。**意味を変えたら鍵も変える。** ②「遊べる」だけだと、倉庫のものは遊べないように読める。→ **「すぐ遊べる」**にし、空のときは何をすればいいかを出す（`「倉庫」の札から選んで遊ぶと、取り寄せた分がここに残ります`） |
| 48 | ~~更新が届かない（朝から）／7171本で重い~~ | **済（2026-08-31・本人「今日は朝から君の更新、全然反映されない」）。**<br>**①見張り番が新しい版を見に行っていなかった。** 理由は2つ。`updateViaCache` を指定していなかったので**見張り番そのものがブラウザの控えから配られ**（GitHub Pages は `max-age=600`）、さらに**開きっぱなしのページは更新を確かめない**（ブラウザが確かめるのは、たいてい遷移したとき）。朝から開いたままなら、いつまでも古い。→ `updateViaCache:'none'` を指定し、**自分から確かめる**（5分ごと＋画面に戻ったとき）。新しい版になったら一度だけ読み直す（#40）。<br>**②自動の読み込みが効いて 7171 本になった。** ファミコン3380・PCエンジン1163・スーファミ671・PC-98 595・DS 593・MD 407・GG 192・GBA 121・N64 35・MSX 14。**3000枚の札を一度に組み立てると iPhone が固まる**ので、**120枚ずつ区切って**出し「もっと見る」で伸ばす。3365本で 20ms・札258枚まで落ちた |
| 47 | ~~絞り込みが分からない → 耳読と同じ3つの札に~~ | **済（2026-08-31・本人「全然わからん。これと同じ作りでいいやん」）。** 耳読の書棚は `[読める本 (33)] [蔵書] [仕込み (214)]` の**名前＋数の札**1つで済ませている。こちらは「倉庫にある分だけ」「取り寄せた分」という**入り切りのボタン2つ**＋説明文で、押すまで何が起きるか分からなかった。<br>→ **`[遊べる (95)] [ぜんぶ (191)] [倉庫に無い (96)]`**。名前と数が並び、いま見ているものが塗られる。**説明文は消した**（札が名乗っているので要らない）。<br>数は**いま見ている機種の中**で数える。取り寄せ済みは札にせず `●` の印だけ残した（4つに増やすと耳読の潔さが崩れる） |<br>**置き所を間違えていた（本人「どこにあんの」）。** 帯の真ん中に埋めていたので、横に流れて見えなかった。耳読は**題名のすぐ隣**にある。→ **帯の先頭**に出し、横に流しても**左に残る**ようにした（`order:-1` と `position:sticky`）
| 46 | ~~「手元」という三つ目の言葉／取り寄せの絞り込みが PC-98 に効いていなかった~~ | **済（2026-08-31・本人「手元?棚では?」「フィルタもうまく動いていない」）。**<br>**①言葉。** 倉庫・棚のほかに「手元」を使っていて浮いていた。この棚には**取り寄せる**という言い方が既にあるので、**「取り寄せた分」**に統一。<br>**②絞り込みが壊れていた（本人の指摘どおり）。** しまう鍵が機種で違う —— ファミコン系は**本の id**、PC-98 は**ディスクの fileid**（遊ぶ画面が別なので置き方も別々に育った）。`S.here[g.id]` しか見ていなかったので、**PC-98 には一度も当たっていなかった**。`gotIt()` で吸収した。<br>**③棚に何があるか分からなかった。** 状態を3つに決めて出す: `● 取り寄せ済み`（圏外でも遊べる）／`倉庫に無い`（遊べない・旧「未アップ」）／印の無いもの（倉庫にある・押すと取り寄せる）。棚の帯の下に読み方を1行、入口の札に `倉庫に 95 / 191 本 ・取り寄せ済み 28`。<br>**踏んだ罠: 置換が2回、黙って空振りした。** 目印の文言を先に変えていたのに気づかず、`gotIt` の**定義だけ入らず使う側だけ入った**（`gotIt is not defined`）。**置換したら入ったことを確かめる。** |
| 45 | ~~置き場を本人に登録させていた~~ | **済（2026-08-31・本人「場所も分かってんだから読んどきなよ」）。** そのとおり。道が分かっているのに、根から1階ずつ降りさせるのも、登録させるのも、ただの手間だった。<br>→ **こちらから探しに行く。** `listfolder?path=/EMU/ROM` のように**道で直に聞く**（1回で済む）。`/EMU/ROM`・`/EMU/BIOS`・`/ROM`・`/roms`・`/Games`・`/ゲーム棚` を試し、見つかった分を足してそのまま走査する。無い道は黙って飛ばす。<br>**初回に自分で走る**（`autoDone` で1回だけ）。あとから「よくある置き場を探す」で呼び直せる。<br>道を直に打つ口も付けた（降りるのが遅い倉庫での逃げ道）。<br>**踏んだ罠: 関数を `screenPlaces` の中に入れてしまった。** 字下げを揃えていなかったので外にあるように見えたが、実際は入れ子で `autoPlaces is not defined` になった。**字下げは根拠にならない。** |
| 44 | ~~場所選びが「見ています…」のまま固まって見える~~ | **済（2026-08-31）。** 黙って待たせていたので、**動いているのか死んだのかが分からなかった**。<br>→ **秒を出す**（`見ています… 12 秒`）。しくじったら理由と「もう一度」を出す。待ち時間も 25秒 → 60秒 に伸ばした（倉庫が大きいと根の一覧に時間がかかる）。場所選びは3か所（倉庫の場所・棚に上げる・重複）にあるので全部同じにした。<br>**版を見出しに出した**（設定の奥に置いたら「どこに表示?」と聞かせてしまった） |
| 42 | ~~毎回「場所を選んで探す」をさせていた~~ | **済（2026-08-31・本人「もう持ってるでしょ」）。** 倉庫を見直した時点で在処つきの一覧は手に入っているのに、「棚に上げる」で**同じ所をもう一度歩かせて**いた。<br>→ `scanAll` が在処つきの一覧も残し、「棚に上げる」は**開いた時点でそれを出す**（`EMU の分をそのまま出しています（◯時◯分に調べた分）。探し直す必要はありません。`）。ボタンは「調べ直す」「ほかの場所を調べる」だけ。<br>ついでに `scanAll` の中で `indexFolder`（名前→fileid だけ）を呼んでいたのをやめ、`scanFolder` に一本化（在処を捨てていたから、もう一度歩く羽目になっていた）。<br>**版を設定に出した。** 「直したのに変わらない」を毎回やり取りしないため（公開先は v51 を配っていたのに、端末の見張り番が古いままだった） |
| 41 | ~~一覧が縦に長い → フォルダの木にする~~ | **済（2026-08-31・本人「フォルダ構造をもってツリー表示できないかな」）。** 機種で束ねても1つの束が数百行になり、縦に長いままだった。<br>倉庫は `/ROM/家庭用ゲーム機/任天堂/NINTENDO64` のように整理されているので、**その形をそのまま出す**。枝ごとに件数、開いた枝は端末に覚える。**探している最中は木にしない**（平らに出す）。<br>**枝ごと選べる**（「この下を選ぶ」）—— メーカーの棚を丸ごと寄せたいときに要る。もう一度押すと外れる。<br>「台帳にある／無い」の2段組みをやめ、**1本の木**にして各行に印を付けた（同じものを2回並べると、それだけで倍の長さになる） |
| 40 | ~~調べた結果が毎回消える／直しても画面が古いまま~~ | **済（2026-08-31・本人「毎回リセットされるの?」「3回目」）。**<br>**①結果を捨てていた。** 1万2千ファイル歩いた結果を、画面を離れただけで失っていた（`S.gather` が memory だけ）。端末に残し、`ROM ◯件・◯月◯日◯時に調べた分` と出す。<br>**②直しても古い画面のままだった。** 見張り番（`sw.js`）は毎回取りに行く作りだが、**入れ替わったことを画面に知らせていなかった**ので、開きっぱなしだと古いまま。→ 入れ替わったら**一度だけ読み直す**。控えの名前にも版を入れた（`pg-shell-v3`）。<br>**先に控えるのは版のない名前だけにした** —— `app.js?v=21` が書きっぱなしで残っていた。版つきの名前を書くと必ず直し忘れる |
| 39 | ~~重複を片付ける~~ | **済（2026-08-31・確かめ待ち＝A17）。** 倉庫の場所 →「重複を片付ける」。**消す操作なので安全側に倒した。**<br>①**大きさが違うものは重複と見なさない。** 同じ名前でも中身が別物のことがある（版違い・壊れた控え）。消す候補から外し、件数だけ出す ②残す1本は**置き場で決める** —— `重複`・`隔離`・`バックアップ`・`old` の下は後回し、次に浅い所、それでも並べば名前順。**●で選び直せる** ③消す前に**何を何件どれだけ**消すか出して確かめる ④消したら戻せないと明記<br>試した: `マリオ.nes` が3か所（同じ大きさ）→ `/ROM/任天堂` を残し、`/Backup/old` と `/その他/整理隔離/重複` を消す候補に。`三國志.fdi` は大きさ違いなので触らない |
| 38 | ~~「棚に上げる」の一覧が探せない~~ | **済（2026-08-31・本人「こんなにずらずら並べても探せない」）。** 403件を平らに並べていた。**棚には畳む仕組みを入れたのに、こちらに入れ忘れていた**（倣うと言ったのに片方だけ）。<br>入れたもの: ①**機種で畳む**（件数つき・閉じた束は端末に覚える・**探している最中は畳まない**）②**探し口**（題名でも置き場でも絞れる）③**重複の印** —— 同じ名前が倉庫の何か所にもある（`/その他/整理隔離/重複/…`）。並べただけでは気づけないので `重複 2か所` と出す ④「全部選ぶ」は**見えている分だけ**（403件を丸ごと選ばせても選び直すのが大変）⑤1束300件で頭打ち（それ以上は絞ってもらう）<br>**教訓: 一覧を作ったら、棚と同じ作法（畳む・探す）を必ず通す。** |
| 37 | ~~設定が分かりにくい（入口が重複）~~ | **済（2026-08-31・本人「わかりにくい」）。** 機能を足すたびに行を増やしたので、**同じことを指す入口が4つ**あった: `倉庫のフォルダ`（表示だけ）・`見に行く場所`・`フォルダを選び直す`・`倉庫を見直す`。上げる道も `棚に上げる` と `Mac から上げる` の2つが並んでいた。<br>→ **「倉庫の場所」1つにまとめた。** 場所の一覧・★で上げ先・足す・外す・**上げ先の付け替え**・見直しを全部そこで済ませる。`Mac から上げる` は「棚に上げる」の中へ（元々そこから行ける）。<br>設定は **14行 → 9行**。<br>**教訓: 足すときに、既にある入口と重ならないか見る。**行を増やすほうが楽なので、放っておくと必ずこうなる |
| 36 | ~~言葉づかい: pCloud は「倉庫」~~ | **済（2026-08-31・本人の指定）。** `ROM を置いたフォルダ` を選ぶ所で「**ここを棚にする**」と言っていたのが変だった —— そこは pCloud のフォルダを決める所で、棚ではない。<br>**倉庫** … pCloud。ROM の置き場。／**棚** … この画面。並べて遊ぶ所。<br>「棚のフォルダ」のような**どちらとも取れる言い方はしない**。`ここを倉庫にする`・`倉庫にある分だけ`・`まだ倉庫に無い本`・`倉庫を見直す`・`倉庫へ上げる/から下げる` に統一。「棚に上げる」は棚に出るようにすることなので、そのまま |
| 34 | ~~棚が埋まらない本当の原因~~ | **済（2026-08-31）。** 「ROM はもっとある、特に PC-98 はほぼ全部ある」の中身は2つ。<br>**①走査が丸ごと失敗していた。** `scanAll` は `indexFolder` を呼び、そこで直に `recursive:1` を頼んでいた。**この口はあなたの pCloud が断る**（#30 で `scanFolder` だけ直したが、`indexFolder` は素のままだった）。1か所ごとに黙って握り潰していたので、棚が空でも理由が出なかった。→ `indexFolder` を `scanFolder` の上に載せ、**入口を1つにまとめた**。あわせて**どこが何件だったか必ず出す**ようにした。<br>**②PC-98 の拡張子を取りこぼしていた。** `.fdi/.hdi/.d88/.hdm/.xdf/.nfd` しか見ていなかった。NP2kai が読むのは **FDI・FDD・HDM・TFD・XDF・DUP・2HD・D88・D98・88D・NFD・HDI・THD・NHD・VHD・SLH・HDD・DIP**。全部拾うようにした。MSX（`.dsk/.mx1/.mx2/.cas`）も足した。<br>拾う拡張子は `EXT_SYS` に一本化（2か所に書いていたので必ずずれる） |
| 35 | ~~「棚に上げる」の探す場所は別に指定する~~ | **済。**「見に行く場所」は**置いたまま棚に出す**ための登録、「棚に上げる」は**寄せたいものを探す**ための一時の指定。同じにすると、寄せたくない置き場まで巻き込む。専用の場所選びを付けた（フォルダを降りながら「この階に ROM が◯個」も出す） |
| 33 | ~~入口を機種にする~~ | **済（2026-08-31）。** 機種が14に増え本数も350を超えたので、平らに並べても選べない。<br>入口は**機種の札**（その機種でいちばん遊んだ本の箱絵を薄く敷き、上にロゴ）。札には **`棚にある / 全部` 本**を出す（例: `127 / 191 本`）。押すとその機種の中へ。<br>探すのは**機種をまたぐ**（入口の探し口に打つと、ぜんぶの一覧へ移って続きが打てる）。「ぜんぶ見る」も残した。<br>機種の中では**ジャンルで畳む**（束が1つでは畳む意味がないので、`sys` を選んでいても中では genre に読み替える） |
| 32 | ~~見つけた ROM から棚を起こす~~ | **済（2026-08-31）。** 「見に行く場所」を見たとき、**台帳に無い ROM をその場で棚に起こす**。過去の整理リストを探したが手元（作業場・Obsidian）には無く、pCloud の中にあるらしい。**リストが無くても中身は見えるので要らない。**<br>PC-98 は1本＝複数枚なので、`三國志５_A.fdi` `_B` `_C` のように末尾が1文字違いなら**束ねて1本**にする。`BIOS`・`font`・`sound` は外す。<br>**機種を増やした。** 台帳に載っているのは Mac にあった5機種だけだが、pCloud には セガ・NEC・SNK・MSX もある。メガドライブ・マスターシステム・ゲームギア・PCエンジン・ワンダースワン・ネオジオポケット・ゲームボーイ・GBA・バーチャルボーイを足した。<br>**PSP と PS1 は出さない**（どちらも `.iso` で見分けられない）。<br>`play.html` も端末側の分（`pg.extra`）を引くようにした（台帳だけ見ていたので「台帳にありません」で止まっていた） |
| 31 | ~~ROM は `/EMU/ROM/…` にメーカー別で整理してあった~~ | **済（2026-08-31）。** 棚のフォルダ（`/ゲーム棚`）とは別。**そこへ移させるのは筋が悪い**（せっかくの整理を崩す）。**その場所も見に行けば済む** —— 遊ぶときに使うのは fileid なので、どこに置いてあっても関係ない。<br>設定 →「**見に行く場所**」。棚のフォルダに加えて何か所でも足せる。走査は全部を回して名前→fileid を合わせる（同じ名前なら棚のフォルダを優先）。上げ先と記録の置き先は棚のフォルダのまま。<br>**根っこから歩かせない。** 全体を歩いたら書類も控えも巻き込んで**5万2千ファイル**を数え始めた。「棚に上げる」も、挙げた場所だけを見るようにした |
| 30 | ~~「pCloud の中を探す」が `Invalid request.` で断られる~~ | **済（2026-08-31）。** 一度の `listfolder?folderid=0&recursive=1` で全部取ろうとして断られた。<br>直したこと: ①**断り文句に番号を添える**（`Invalid request.` だけでは調べようがない）②`folderid=0` が駄目なら `path:"/"` で頼み直す ③どちらも駄目なら**自分で歩く**（1階ずつの `listfolder`。棚のフォルダ選びで通っている呼び方なので確実。進み具合を出す）<br>同じ断られ方を作って確かめた: 再帰2通りとも断られたあと、歩いて全件を在処付きで拾えた |
| 27 | ~~「棚に上げる」に改名して表に出す~~ | **済。**「棚を編む」は変。設定の奥にあったのも間違い。棚の帯に**「＋ 棚に上げる」**を出した。押すと pCloud の中を探す画面へ（Mac から上げる道はその先に置いた） |
| 28 | ~~台帳に無い ROM が捨てられていた~~ | **済。**「pCloud にはもっとあるはず」の正体。**台帳は Mac の中身から起こしたもの**なので、それ以外の道で pCloud に入れた分は載っていない。載っていないものを画面から**捨てていた**（`if (!item) continue`）。<br>→「台帳に無い」束として出し、移すと**この端末の棚に足す**（`pg.extra`）。機種は拡張子から当て、題名はファイル名から起こす。置き場（GitHub）には触らない |
| 29 | ~~機種・ジャンルで畳む（耳読に倣う）~~ | **済。** 365本を平らに並べていたので見にくかった。耳読の書棚と同じ作法: 見出しに `▾/▸` と件数、**閉じた束は端末ごとに覚える**、**探している最中は畳まない**（探した意味がなくなる）。既定は機種で畳む。<br>直した誤り: **`hidden` だけでは畳めない。** `.grid{display:grid}` のほうが強くてブラウザ既定の `display:none` を押し返す（見出しの三角だけ変わって中身が残った）。`.grid[hidden]{display:none}` を足した |
| 26 | ~~pCloud から棚へ（耳読に倣う）~~ | **済（2026-08-31・確かめ待ち＝A16）。** 設定 →「pCloud から棚へ」。<br>**なぜ作り直したか。** 既存の「棚を編む」は**Mac のフォルダを選ばせる**造りで、**iPhone からは何もできなかった**（Safari は `webkitdirectory` に対応していないのでフォルダを選べない）。耳読の書棚は「pCloud にある本を並べて、その場でボタンを押す」だけで元を選ばせない。同じ形にした。<br>**すでに pCloud にあるものは上げ直さない。** 落として上げ直すのは時間も通信も無駄。`renamefile` に行き先を渡せば**向こう側で動く**（一瞬）。元を残す「写す」（`copyfile`）も付けた。<br>共通部品に `moveFile` と `scanFolder` を追加（`indexFolder` は名前→fileid だけなので、**同じ名前が別の場所にもあると潰れる**。棚の外を拾うには在処が要る）。<br>「棚を編む」側にも**「ファイルを選ぶ」**を足した（iPhone でもまとめて選べる）。選んだ分は**足す**ので、何度かに分けて選べる |
| 23 | ~~機種をロゴで出す~~ | **済。** 字の札より一目で分かる。Commons から取って**白抜きに揃える**（元の色は赤・黒・多色でばらばら。暗い札に載せると読めるものと読めないものが出る）。<br>踏んだ罠: ①**PC-98 のロゴは「PC-98」では出てこない。** `PC9800` で引くと `PC9800 logo 1982.svg` が出る ②**PSP は黒地に白抜き**なので、白くするだけだと真っ白の板になった。四隅の明暗を見て、どちらを抜くか決める ③続けて叩くと 429 で断られる。間を空ける<br>本人の直し: **もっと大きく**（13px では読めなかった → 22px、狭い画面は17px）。**枠を付けない。** 札にすると絵の上で四角が目立ちすぎて箱絵より先に目に入る。白抜きのロゴを直に置き、影だけで浮かせる |
| 24 | ~~詰め合わせの絵を合成~~ | **済。** そういう箱は世に無いので、中身の絵を重ねて作る（`tools/pack-cover.py`）。ブランディッシュ1.2.3 |
| 25 | ~~箱絵を手で入れる口~~ | **済。** 設定 →「箱絵を直す」。探しても出てこない本と、切り出しを外した本を、その場で差し替えられる。**置き場には送らず端末の中だけ**に置くので、押し出す手間が要らない。入れる絵は320pxに縮める（写真のままだと置き場を食う） |
| 20 | ~~パッケージ写真を四角く切り出す~~ | **済（2026-08-30）。** `tools/crop-cover.py`。売り買いの場の写真は机や布が一緒に写るので、棚に並べると大きさも位置もばらばらに見える。<br>**出どころを記録して、写真から取ったものだけ切る**（`csrc:"検索"`）。まとめ場の箱絵はもともと綺麗なので触らない（最初は全部を対象にして219枚を切ろうとした）。<br>直した誤り: **行と列の合計では箱を掴めない。** 明るい背景や写り込みに負けて一部しか囲えず、55枚中10枚以上を切り損ねた。→ **一続きの塊として捉え、いちばん大きく・いちばん四角いものを採る**（`scipy.ndimage.label`、しきい値を3通り試す）。<br>**第二段（本人の「これで限界?」を受けて）: 四角く囲うのをやめ、角を取って起こす。** 傾いた箱・斜めから撮った箱は、囲うだけでは机が残るし箱も歪んだまま。→ ①`approximate_polygon` で**角が4点**取れれば射影変換で起こす（台形の歪みまで直る。ただし**82枚中4枚しか取れない**）②取れないときは**包むいちばん小さい傾いた四角**（回転カリパス）で起こす。これで**44枚中21枚**を起こせた。<br>**まっすぐなものは起こさない**（粗くなるだけ）。囲みに比べて7%以上小さいときだけ。<br>元は `covers-orig/` に残る（`--undo` で戻る）。<br>**まだ残る限界**: 1枚に商品が幾つも写っているとき、どれが箱かは色と四角らしさで推すしかない |
| 21 | ~~Steam / プロジェクトEGG の絵も最後の手段に~~ | **済。** 当時の箱の写真が無くても、いま売られている版の絵はある。言い方の4番目・5番目に足した |
| 22 | ~~棚の機種が分かりにくい~~ | **済。** 小さい灰色の札では並べたときに読めなかった。**機種ごとに色を決め、字も大きく**した（FC 赤／SFC 藍／N64 緑／DS 銀／PSP 紫／98 NEC の青）。絵の下辺にも同じ色を3px敷いて、札が絵に紛れても並びの中で拾えるようにした |
| 19 | **公開リンクを `<audio>`／ROM に直渡しできるか（実測）** | ブラウザから `getfilepublink` が通るなら、中継所なしでも**頭出しの効く速い道**になる。通れば音楽棚の中継所依存も外せる。30分で白黒つく。通らなければ #17 の道で打ち止め |

## 片付いた問い

| # | 問い | 決着 |
|---|---|---|
| Q1 | ジュークボックスを戻すか | **音楽プレーヤー側の話。こちらの担当外**（2026-08-30）。ゲーム棚からは触らない。<br>ただし `pcloud-music` に次の変更が入ったままなので、音楽棚を触るスレへの申し送り事項：<br>`app.js` に `jukePool()` / `jukeMoodList()` を追加、`screenJuke()` にジャンル・雰囲気の選択列と handler、`JB` に genre/mood。`index.html` に `.jb .pickrow` の見た目と `app.js?v=100`。`sw.js` の版。`README.md` に説明。 |
