# ゲーム棚 引き継ぎ書

2026-08-31 時点。**次に触る人が、同じ穴を掘り直さないための書き置き。**
細かい経緯は `TODO.md`（番号つき・72件）に全部ある。ここは要点だけ。

---

## 1. これは何か

pCloud に置いた ROM を、**ブラウザだけで**遊ぶ棚。マウントは一切使わない。

| | |
|---|---|
| 置き場 | `~/claude code/pcloud-games/` |
| 公開先 | https://dshino9.github.io/pcloud-games/ |
| 中継所 | https://gamedana.d-shino.workers.dev （素の `relay.js`・合言葉なし） |
| 共通部品 | `~/claude code/shelf-core/`（**正本はここだけ**。各アプリの `core/` は配られたもので触らない。`sh tools/sync-core.sh` で配る） |
| 版 | 見出しの右に出る（`v102`）。**公開されたか必ず確かめること**（後述） |

---

## 2. 本人が決めた概念（ここを間違えると全部ずれる）

```
倉庫   pCloud にある ROM 全部。あなたの整理（/EMU/ROM/…）は崩さない
 ↓ 棚に入れる／書庫へ下ろす   ← **状態はこれ1つだけ**
棚     よく遊ぶ本。**ROM 本体と関係ファイル**を持つ（/ゲーム棚/棚/<本>/）
書庫   下ろしたもの。**関係書類ごと一箇所**（/ゲーム棚/書庫/<本>/）
```

**端末には入れない。** ブラウザを開けばどこでも同じ環境。
端末の控えは速さのための一時置きで、**状態として画面に出さない**。

### 言葉づかい（本人の指定・変えないこと）

| 語 | 意味 |
|---|---|
| **倉庫** | pCloud。ROM の置き場 |
| **棚** | 遊ぶと決めた本。`/ゲーム棚/棚/` |
| **書庫** | 下ろしたもの |
| **コア** | エミュレータ本体（「心臓」は不可） |
| **Aドライブ／Bドライブ** | PC-98 のフロッピーの口（「1番の口」は不可） |
| 使わない語 | 「手元」「取り寄せ」「インストール」 |

会の名前（Flying Dreams / FD）は**成果物にも会話にも書かない**。

---

## 3. 倉庫の形（実測）

```
/EMU/
  BIOS/
  ROM/
    パソコン/PC-98/PC98/PC98 Disk/<メーカー>/【PC98】<題名>/*.fdi (+ 箱絵)
                              /PC98 HDD/…
              /PC-98/Collection/…            ← 目録の HTML がある
    家庭用ゲーム機/<メーカー>/<機種>/*.nes 等
    アーケード/  パソコン/MSX/
  エミュレータ/  その他/整理隔離/重複/  資料/
/ゲーム棚/            ← 棚・書庫・記録の置き先
```

- **メーカーはフォルダが正**（`Alice Soft` `Arsys` `Artdink` …数百）。題名から当てる表（`makers.js`）は当てにならない補助
- **1本＝1フォルダ**（PC-98）。`A.fdi` `1.fdi` が別の本に山ほどある
- 走査で **35,544 ファイル**。うち棚に出ているのは 7,171 本

---

## 4. いま動くもの（実際に動かして確認した）

入口／一覧／本の頁／設定／棚と倉庫／箱絵を直す／動きの記録／押した記録／中継所／
ディスクの道具箱（整形済みブランクを作れる）／棚に上げる／重複を片付ける／
版を選ぶ／倉庫を見直す（潰れずに本を起こす・倉庫の絵を結び付ける）／目録を読む／
書庫へ下ろす（フォルダごと移動）

---

## 5. できていないもの（**引き継ぐ人はここから**）

### ① 端末に置いていて、別ブラウザで消える — 6件

`pg.extra`（見つけた本の台帳）・`pg.renamed`（目録で直した題名）・
`pg.fdpick`（ディスクの組み合わせ）・`pg.ver`（選んだ版）・
`covers-v1`（手で入れた箱絵）・`roms-v1`（セーブが書かれたディスク）

**全部 `/ゲーム棚/棚/<本>/` へ移すこと。** これが最大の未達。

### ② 指示に反している — 2件

- **棚に ROM 本体を写す**処理を、私が外しかけている（`app.js` の `stock()`。**手元に未公開の編集あり**）。本人の指示は「持て」。**戻すこと**
- **書庫を見る画面が無い**（本の頁からしか辿れない）

### ③ 未着手 — 3件

- ディスクの**中身をいじる**（`pc98disk.js` は読み取りのみ）
- 見つけた本に**ジャンルが無い**
- **音と速さの揺らぎの根治**（本家は AudioWorklet に差し替えている）

### ④ 作ったが実物で未確認 — 5件

見つけた PC-98 の起動（v101）／倉庫の中の絵（v98）／重複の片付け／
棚に入れる・書庫へ下ろし／**そもそも遊べるか**

### ⑤ 合鍵が無くて進めない — 4件

iPhone の激重／1分で落ちる／箱絵の捜索（残り数千本）／動きの記録を読む

→ 本人に一度だけ実行してもらう（パスワードは本人が打つ。こちらは合鍵だけ預かる）:

```bash
python3 tools/runs.py --login
python3 tools/runs.py            # 動きの記録
python3 tools/runs.py --shelf    # 題名の一覧（箱絵の捜索用）
```

---

## 6. 踏んだ地雷（**同じ穴を掘らないこと**）

### pCloud

- **`listfolder?recursive=1` を断る口がある。** `folderid=0` → `path:"/"` → **1階ずつ歩く** の順に降りる（`shelf-core/pcloud.js` の `scanFolder`）。`indexFolder` もこの上に載せる
- **根っこから歩かせない。** 全体を歩くと 5万2千ファイルを数え始める
- **中継所が無いと遊べない。** 中継所なしの道（`file_open`）はこの倉庫では **2003 Access denied**。既定で中継所を指すこと
- 配信元（`ptok2.pcloud.com`）は **CORS を返さない**
- **NFC に揃えて比べる**（macOS の名前は NFD）
- 走査に上限を置くなら**当たったと言うこと**（黙って打ち切ると「もっとあるはず」になる）

### 身元の持ち方

- **名前を鍵にしない。** `A.fdi` `1.fdi` が別の本に山ほどある。名前で束ねると本が潰れ（49枚で1本という札が出た）、他人の在処が付く（ファミコンの中に PC-98 が出た）
- **在処＋名前**、できれば **fileid 一本**に統一する

### 画面の作り

- **選び方を廃したら、覚えも代入も全部消す。** 3回やらかした（`view`・`fold` の覚え・`fold` の代入）。`grep` で名前を全部当たること
- 畳んだ束の覚えは**束ね方ごとに分ける**（`maker/光栄`）。名前だけだと別の束ね方まで隠す
- **`hidden` だけでは畳めない**（`.grid{display:grid}` が勝つ）
- **勝手に階を飛ばさない。** 「1本道は要らない」と省いたらメーカーの階を通り過ぎた
- 7千本を一度に組み立てない。**閉じている束は組み立てない**

### 見張り番（sw.js）

- `updateViaCache:'none'` が要る。無いと**見張り番自身が控えから配られる**
- **開きっぱなしのページは更新を確かめない。** 5分ごと＋画面に戻ったときに自分で確かめる
- 控えの名前に版を入れる。先に控える一覧に**版つきの名前を書かない**（直し忘れる）

### エミュレータ

- **隠れている頁では動かない**（rAF が止まる）。ブラウザ枠・ヘッドレスは全部これ。**不具合ではない**
- `EJS_gameUrl` は **File** で渡す（blob URL だと拡張子が消えて灰色のまま）
- PC-98 のコアは自前ビルド（`tools/build-np2kai.sh`、当て木4つ）
- **内部で試すときは音を止める**（`--mute-audio`・`EJS_volume=0`）

---

## 7. 本人が求める作法

1. **要件は番号を振って一覧に**（`TODO.md`）。会話に埋めない
2. **一度に一つ。確かめてもらってから次へ**
3. **確かめずに「できた」と言わない。** 再現して直ったのを見てから言う
4. **置換したら入ったことを確かめる**（黙って空振りする）
5. **ある程度できたらモックを見せる。** こちらから確認用の画面（リンク）を出す。本人にスクショを撮らせない
6. `~/pCloud Drive/` のマウントは**読まない・書かない・存在を確かめない**（`ls` も含む。カーネルで固まり `kill -9` も効かない）
7. 確認用サーバーは `.claude/launch.json` から `preview_start`
8. 公開したら **`curl` で版が出たか確かめる**（GitHub Pages は数分遅れる）

---

## 8. 次にやること（本人と合意した計画）

| 段 | 中身 |
|---|---|
| **1** | **選ぶ画面をゼロから作り直す**（つぎはぎをやめる）。Steam・EGG・Playnite に倣い、左＝機種→メーカー、中央＝一覧（札／行）、右＝選んだ本の札。画面を移らずに選べる。ジャンル欠落もここで扱う。**モックのリンクを出して承認を得てから実装** |
| **2** | ①の6件＋②の2件 ── 置き場を `/ゲーム棚/棚/` に一本化、ROM も持つ、身元を fileid に統一、書庫を見る画面 |
| **3** | ④の5件を**実物で通す**（遊べるところまで） |
| **4** | ⑤の4件（合鍵が要る） |
| **5** | ディスクの書き換え・音の根治 |

**モックの下書き**が
`/private/tmp/claude-501/…/scratchpad/library-mock.html` にある（未公開）。
消えても構わない ── 設計の骨は上に書いた通り。

---

## 9. ファイルの地図

| ファイル | 中身 |
|---|---|
| `index.html` | 骨と見た目。**版の番号はここ**（`app.js?v=NN`） |
| `app.js` | 棚の本体（画面の振り分け・一覧・走査・棚/書庫・箱絵の捜索） |
| `title.js` | 本の頁 |
| `disks.js` / `pc98disk.js` | ディスクの道具箱 / FAT12 を読む |
| `makers.js` | 題名からメーカーを当てる表（**補助**。正はフォルダ） |
| `play.html` / `play98.html` | 遊ぶ画面（ファミコン系 / PC-98） |
| `sw.js` | 見張り番。**版を上げること** |
| `core/*` | 配られた共通部品（**触らない**） |
| `tools/` | `fetch-covers.py`（箱絵）・`crop-cover.py`（切り出し）・`runs.py`（記録）・`build-np2kai.sh` |
| `TODO.md` | 番号つきの経緯。**まず読むもの** |
