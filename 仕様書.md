# ゲーム棚 仕様書（現状）

2026-08-31 時点の**実装されている姿**。設計の理想ではなく、いま動いているもの。
守れていない要件は「未達」として明記する（引き継ぎ書と対で読む）。

---

## 1. 全体像

```
利用者 ── ブラウザ ──┬── GitHub Pages（棚そのもの。HTML/JS）
                     ├── api.pcloud.com（一覧・上げる・消す・動かす）
                     └── 中継所 Cloudflare Worker（ROM の中身を取る）
                                    ↓
                              pCloud（倉庫・棚・書庫）
```

- **サーバーを持たない。** 棚は静的ファイルだけ。中身は全部 pCloud
- **マウントは使わない**（固まるため）。すべて HTTP API
- **中継所が要る**。pCloud の配信元は CORS を返さず、`file_open` はこの倉庫では `2003` で塞がっている

---

## 2. 置き場

| 名 | 実体 | 中身 |
|---|---|---|
| 倉庫 | 登録した pCloud のフォルダ（既定 `/EMU`） | ROM 本体。利用者の整理のまま |
| 棚 | `/ゲーム棚/棚/<題名>/` | ROM ※・`箱絵.jpg`・`メモ.json`・`セーブ/` |
| 書庫 | `/ゲーム棚/書庫/<題名>/` | 下ろしたものを丸ごと |
| 記録 | `/ゲーム棚/_記録/` | `<端末>.json`（動きの記録）・`棚.json`（題名の一覧） |

※ **ROM を棚へ写す処理は現在コードにあるが、私が外しかけた未公開の編集がある。指示は「持て」。**

---

## 3. 台帳（本の一覧）はどこから来るか

3つの出どころを1つに混ぜている。

| 出どころ | 中身 | 置き場 |
|---|---|---|
| `games.json` | OpenEmu 由来 253本（FC/SFC/N64/DS/PSP）。箱絵・ジャンル付き | 置き場（Git） |
| `pc98.json` | RetroArch 由来 85本 | 同上 |
| `pg.extra` | **倉庫の走査で見つけた分**（現在 7,171本の大半） | **端末（未達）** |

見つけた本の作り方：

1. 倉庫を歩いて ROM の拡張子のものを拾う
2. **在処＋名前**で見分ける（名前だけだと `A.fdi` が潰れる）
3. **同じフォルダのものを1本**に束ねる（PC-98 は1本＝1フォルダ）
4. 題名は**そのフォルダがその本だけのもの**ならフォルダ名、でなければファイル名
5. 同じフォルダの絵（`jpg/png/…`）を箱絵として結び付ける

対応する拡張子：

```
FC   nes fds unf         SFC  smc sfc fig swc      N64  n64 v64 z64
DS   nds                 GB   gb gbc               GBA  gba
MD   md gen smd          SMS  sms                  GG   gg
PCE  pce                 WS   ws wsc               NGP  ngp ngc
VB   vb                  MSX  dsk mx1 mx2 cas
98   fdi fdd hdm tfd xdf dup 2hd d88 d98 88d nfd hdi thd nhd vhd slh hdd dip
（PSP・PS1 は .iso で見分けられないので出さない）
```

---

## 4. 画面

| 経路 | 画面 | できること |
|---|---|---|
| `#/lib` | 入口 | 機種の札（箱絵を敷く・`倉庫n / 全部n`）・機種をまたぐ探し口・棚に上げる |
| `#/sys/<機種>` | 一覧 | 札 `棚 / 倉庫 / 倉庫に無い`・探す・畳む（機種/ジャンル/メーカー）・並べ替え・区切って出す |
| `#/t/<id>` | **本の頁** | 遊ぶ・棚に入れる・書庫へ下ろす・版を選ぶ・箱絵を差し替える・ディスクの組み合わせを忘れる・中身の一覧 |
| `#/all` | ぜんぶ | 機種をまたぐ一覧 |
| `#/set` | 設定 | 版・pCloud・台帳・棚に上げる・棚と倉庫・箱絵を直す・ディスクの道具箱・動きの記録・押した記録 |
| `#/places` | 棚と倉庫 | 棚の置き先／倉庫の場所（足す・外す・自動で探す・見直す・重複を片付ける・目録を読む）・倉庫の中身（拡張子ごと） |
| `#/gather` | 棚に上げる | 倉庫の中でフォルダの木を辿って、選んで寄せる（重複の印つき） |
| `#/edit` | Mac から上げる | 手元のファイルを上げる／倉庫から下げる |
| `#/dupes` | 重複を片付ける | 同じ名前・同じ大きさの組を出し、1本残して消す |
| `#/covers` | 箱絵を直す | 自分の絵を入れる |
| `#/disks` | ディスクの道具箱 | 中を見る・整形済みブランクを作る・複製・改名・消す・取り出す |
| `#/runs` | 動きの記録 | 10秒ごとのコマ数・端末・落ちた印 |

**どの画面からも**見出しの `←`（一つ上）と `棚`（トップ）で戻れる。

---

## 5. 遊ぶ

### ファミコン系（`play.html`）

- EmulatorJS（`cdn.emulatorjs.org/stable`）。コアは台帳の `core`
- ROM は中継所ごしに取り、`new File([blob], 名前)` で渡す（**blob URL では拡張子が消えて動かない**）
- 台帳に無い本は端末の `pg.extra` から引く
- 5秒ごとに動きの記録を書く。ちゃんと閉じたときだけ「終わった」印

### PC-98（`play98.html`）

- コアは自前ビルドの NP2kai（wasm。JSPI 版と ASYNCIFY 版）
- **起動前に A/B ドライブを選ぶ**。他の本のディスク・ブランクも足せる
- 3枚目からは `diskdrv_setfddex` で差し替え。**入れ替えでリセットしない**
- セーブはディスクそのものに書かれる → 終了時に書き戻す
- 台帳に無い本は `pg.extra` から題名とディスク一覧を組み立てる
- 指をマウスに訳す・画面のキーボード・本体メニュー（Ctrl+F11）

---

## 6. 箱絵

| 順 | 出どころ | 状態 |
|---|---|---|
| 1 | **倉庫の同じフォルダの絵** | 実装済み・未検証 |
| 2 | libretro のサムネイル置き場（CORS が開いている） | 実装済み。日本語名には当たらない |
| 3 | 手で入れる | 実装済み（**端末に残る＝未達**） |
| 4 | 画像検索（DuckDuckGo の JSON） | **Mac 側の道具のみ**（ブラウザからは CORS で不可） |

写真は箱だけを切り出す（`crop-cover.py`。塊の検出＋角を取って起こす）。

---

## 7. 端末に残っているもの（**未達**）

```
pg.extra    見つけた本の台帳      pg.renamed  目録で直した題名
pg.fdpick   ディスクの組み合わせ   pg.ver      選んだ版
covers-v1   手で入れた箱絵        roms-v1     セーブが書かれたディスク
pg.files pg.pics pg.gather pg.scan pg.plays pg.log pg.fold pg.open pg.sys
```

**上6つは別ブラウザで消える。** 本人の方針（端末に入れない）に反する。

---

## 8. 守っている決まり

- 倉庫の整理は崩さない（移させない・見に行くだけ）
- 消す操作は必ず確かめる。大きさが違うものは重複と見なさない
- 走査に上限があるなら、当たったと画面に出す
- 新しい版になったら一度だけ読み直す（5分ごと＋画面に戻ったとき）
- 音を止めて試す／会の名前を出さない
